services:
  - type: web
    name: dados-tictactoe
    env: node
    region: oregon
    buildCommand: npm install && npx next build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
    autoDeploy: true